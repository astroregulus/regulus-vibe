# Google Analytics
GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Netlify / Decap CMS (Git Gateway — configured in Netlify dashboard, not needed locally)
# GITHUB_CLIENT_ID is only required if using the GitHub backend instead of Git Gateway.
# With Git Gateway (recommended), no client ID is needed here.
GITHUB_CLIENT_ID=
